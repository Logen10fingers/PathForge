from django.contrib import admin
from .models import Skill # Import your Skill model

# Register your models here.
admin.site.register(Skill) # This line registers the Skill model with the Django admin site.